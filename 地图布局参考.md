# 地图布局参考（从 world.tscn 逆向整理）

> 本文是 `world.tscn` 的布局逆向分析，作为**生成新地图的规范**。
> 所有数值都是实测（`get_used_cells()` / `get_cell_atlas_coords()`），不是估算。

---

## 一、素材与图集

### 1.1 崖壁 `World/CliffTileset.png`

- 尺寸 **352×160**，每格 **32×32** → **11 列 × 5 行 = 55 格**
- 语义（按行）：
  | 行 | 内容 |
  |---|---|
  | y=0 | 崖顶（草皮覆盖的顶面）+ 左右转角 |
  | y=1 | 垂直连接段（墙的侧面竖条） |
  | y=2 | 横向墙体主体（深棕岩面） |
  | y=3 | 横向墙体主体（第二道） |
  | y=4 | 底座（左右两侧 x0-3 / x9-10 透明） |
- **碰撞**：55 格中 52 格带整格实心碰撞（`physics_layer_0/collision_layer = 1`，32×32 方块）；**例外**：`(0,4)` `(9,4)` `(10,4)` 无碰撞
- **地形集**：`terrain_set_0`，mode 0，terrain 名 `cliff`

⚠️ **不要用 `set_cells_terrain_connect()`** —— 这个图集的 peering bits 不自洽。实测把一个**纯外环**喂给它，会选出 `(5,1)(6,1)(5,2)(6,2)(4,1)(8,1)(9,1)(5,4)` 等十几种"碎石/立柱"图块，拼出来是一段段**断开的墙，到处是缺口**。

world.tscn 是**手工逐格指定**图块的，只用下面这 13 种（因为干净连续）：

| 位置 | 图块坐标 |
|---|---|
| 顶外层（最上那行） | `(1,0)` 面；两端 `(0,0)` `(2,0)` |
| 顶内层（第二行） | `(1,2)` 面；两端 `(0,1)` `(2,1)`；内转角 `(1,1)` |
| 底外层（最下那行） | `(1,2)` 面；两端 `(0,2)` `(2,2)` |
| 底内层（倒数第二行） | `(1,0)` 面；两端 `(0,1)` `(2,1)`；内转角 `(5,2)` `(6,2)` |
| 左外层 / 右外层 | `(0,1)` / `(2,1)` 面；端头 `(0,0)(0,2)` / `(2,0)(2,2)` |
| 开口断口 | 靠"邻居缺失"判定，自动补端头图块 |

完整实现见 `tools/generate_maps.gd::_place_cliff_tiles()`。

> **world 的崖壁是「纯外环」，内部格数 = 0。** 不要加内部墙 —— 那会让断口端头图块选错，出现缺口。

### 1.2 泥路 `World/DirtTileset.png`

- 尺寸 **176×80**，每格 **16×16** → **11 列 × 5 行 = 55 格**（定义 47 格）
- **无碰撞**（纯视觉）
- **地形集**：`terrain_set_0`，mode 0，terrain 名 `road`
- 含一个 3×3 的 `TileMapPattern` 预设笔刷

### 1.3 草地底 `World/GrassBackground.png`

- **64×64**，两轴无缝平铺
- 用法：`Sprite2D` + `centered=false` + `texture_repeat=ENABLED` + `region_enabled`
- ⚠️ **`region_rect.position` 是纹理偏移，不是世界坐标**。定位用节点的 `position`，`region_rect` 恒从 `(0,0)` 起
- **scale 必须保持 1:1**，非整数缩放会让像素画发虚

---

## 二、道路布局（岔路结构）

world.tscn 的泥路共 **341 格**（16px 格），世界范围 `x -144..784, y 16..608`。

### 结构：1 条主干 + 5 条岔路

```
                    ┌── 北3 ──┐   ┌─ 北2(宽)─┐
   ┌── 北1 ──┐      │         │   │          │
   │         │      │         │   │          │
═══╧═════════╧══════╧═════════╧═══╧══════════╧═══   ← y=18~19 主干道（贯通全宽）
        │                    │
        │ 南1(窄长)          └── 南2 → 尽头广场(变宽)
        │
```

| 路段 | 格坐标 | 宽度 | 说明 |
|---|---|---|---|
| **主干道** | y=18~19，x=0~48 | 2 格（32px） | 贯穿全图，是唯一连续通道 |
| 北1 | x=2~3，y=6~17 | 2 格 | 短岔路，从主干道北上 |
| 北2 | x=20~21，y=1~17 | 2 格 | **最长**，一直通到顶端边缘 |
| 北3 | x=42~46(y1~5) → x=45~46(y6~17) | 5→2 格 | 顶端是个**小广场**，向下收窄 |
| 南1 | x=11~12，y=20~37 | 2 格 | 窄长，直通底部边缘 |
| 南2 | x=32~33(y20~30) → x=28~37(y31~36) | 2→10 格 | 末端**展宽成一个院子/广场** |

### 可复用的规律

1. **1 条贯通的主干道**，通常横向，把地图分成两半
2. **岔路从主干道垂直分出**，北上/南下，**互不平行对称**（间距 8~20 格不等，避免呆板）
3. **岔路长度差异大**（短的 12 格、长的通到边界），制造层次
4. **部分岔路末端展宽成广场/院子**（宽 5~10 格），是放宝箱/篝火/怪群的位置
5. **路宽默认 2 格（32px）**；偶尔 4~5 格做路口或广场
6. **道路不贴墙**：最近处离崖壁内沿还有 6 格（96px）草地边距

> 生成器里的做法：不要用「均匀网格」铺路，要**显式列出一条主干 + 若干条长度/宽度各异的支线**，再给 1~2 条支线加末端广场。

---

## 三、敌人布置（聚簇 + 散兵）

world.tscn 共 **20 只怪**，用「互相 110px 内算同簇」聚类后得到 **7 簇**：

| 簇 | 成员 | 中心 | 类型 |
|---|---|---|---|
| **10 只** | 8 蝙蝠 + 1 甲虫 + 1 哥布林 | (570, 68) | **大怪群**（右上林区）|
| 4 只 | 3 蝙蝠 + 1 甲虫 | (-30, 77) | 中怪群（左上）|
| 2 只 | 2 蝙蝠 | (-111, 307) | 小怪群（左中）|
| 1 只 | 蝙蝠 | (249, 143) | 散兵 |
| 1 只 | 甲虫 | (490, 561) | 散兵 |
| 1 只 | 史莱姆 | (256, 263) | 散兵 |
| 1 只 | 宝箱怪 | (-11, 405) | 散兵 |

### 可复用的规律

1. **不是均匀撒点**，而是**几处热点 + 若干散兵**
2. 热点规模差异大：1 个 10 只的大群、1 个 4 只、1 个 2 只
3. **大怪群里混搭不同兵种**（蝙蝠+甲虫+哥布林），不是清一色
4. 散兵单独放置，彼此离得远
5. 怪群**不放在出生点附近**（最近的散兵离出生点也有 ~150px）

> 生成器里的做法：定义「怪群中心 + 半径 + 数量」和「散兵坐标」两组数据，不要用 `_scatter()` 那种均匀网格。

---

## 四、地图结构骨架

```
MapRoot (Node2D)  ← 必须在 (0,0)，挂 maps/world_map.gd
├─ Background     草地底 (Sprite2D, 64px 平铺)
├─ DirtPathTileMap 泥路 (16px 格, 无碰撞)
├─ CliffTileMap    崖壁 (32px 格, 有碰撞)  ← 唯一的墙
├─ Camera2D        相机
├─ Player          玩家
├─ Grass / Bushes / Tree / Chests   装饰容器 (Node2D)
├─ 敌人            直接挂根下（不要放容器）
└─ Spawns / EdgeTriggers
```

### 崖壁环

- **外圈 2 格厚**的闭合矩形 = 地图边界
- 可以再有**内部体量**（墙、钩状回廊、石柱）制造分区
- **关口 = 从 cell 列表里剔除那几格**，`terrain_connect` 会自动生成干净的端头面
- ⚠️ **2 格厚意味着开口必须整条挖穿**（上下两行 / 左右两列都要剔除），只挖一行仍是一堵墙

### 装饰密度（world 参考）

| 类型 | 数量 | 分布 |
|---|---|---|
| 草丛 | 58 | x -150..751, y 100..545 |
| 灌木 | 24 | x -140..760, y 150..585 |
| 树 | 31 | x -160..760, y 50..585 |
| 宝箱 | 6 | 各村口/角落，`variant` 决定掉落池 |

---

## 五、必须遵守的规则

1. **根节点保持 `(0,0)`** —— 移动它会让相机边界与背景同时错位
2. **不要给地图加 HUD** —— `HUD` 是 autoload，地图里再加一份就显示两套
3. **蝙蝠/甲虫必须直接挂根下** —— 它们死后用 `get_parent()` 生成特效，放进容器会跟着被释放
4. **`region_rect.position` 恒为 `(0,0)`** —— 它是纹理偏移，定位靠节点 `position`
5. **相机 limit = 可走区 ∓ 半视口（160×90）** —— 否则角色走到边缘相机跟不上
6. **背景实绘范围必须盖住相机可见范围** —— 验证用 `bg.get_rect()`，不要直接读 `region_rect`
