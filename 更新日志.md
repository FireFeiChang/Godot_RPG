# 更新日志

## v1.8 - 2026/09/13

### 新增

#### 任务桥段：3 条任务线、6 个可接任务
- 此前只有村长的 `kill_bats` 一条任务。现在：
  - **村庄保卫线**（村长，`world`，串行 3 环）：消灭蝙蝠 ×5 → **草药备荒**（采集止血草 ×10）→ **清剿野兽**（消灭橡子甲虫 ×3）。
  - **东原探险线**（东原冒险者，`east`，串行 3 环）：**黏滑的麻烦**（消灭史莱姆 ×3）→ **哥布林营地**（消灭哥布林 ×3）→ **失落的行囊**（开启宝箱 ×3）。
  - **串行链**：一次只给一条，交付领奖后才解锁下一条；每个任务配 4 段对话（未接 / 进行中 / 可交付 / 已领奖），外加全线走完的收尾对话；接取时给 2 个选项，拒绝也有对应台词。
  - 奖励各不相同：蝙蝠干 / 止血草 / 钱袋 / 罗盘 / 原石 / 望远镜。
- **`world` 的女性冒险者改为指路 NPC**：新建 `NPC/AdventurerChat.dialogue`，4 条可选话题（村长在哪、两条路各通哪儿、草丛能止血、攒盘缠）。她原先与村长**共用 `task_id = kill_bats`**，两个 NPC 会抢同一套对话标题，顺手修掉了。

#### 小地图
- `UI/minimap.gd` + `UI/minimap.tscn`，挂在 `HUD/Root/Minimap`。
- **实时读地图几何，不烘焙贴图** —— 进图时读一次 `CliffTileMap` / `DirtPathTileMap` 的格子与 `EdgeTriggers` 的位置，生成一张 1 像素/格的底图；之后每帧只重画玩家点。地图是生成器产出的，烘焙贴图会随地图改动过期，实时读则永远同步。
- **画的元素**：绿色可行走区、棕色泥路、深色崖壁轮廓、琥珀色**关口**、每帧跟随的**玩家**白点，外加木框/铆钉/底部地图名条。
- **顺手补上了 v1.7 的已知缺口** —— 以前玩家不知道能走出地图，现在关口在小地图上是可见的。
- **面板尺寸随地图变**：world 76×65、north/east 104×69（由崖壁包围盒决定），所以锚点在 `set_map()` 里按地图尺寸算，不是写死在场景里的。
- 地图外（主菜单）随 HUD 整体隐藏。

#### 对话选项支持 W / S 键切换
- 此前只能用鼠标点选（方向键也行，但和移动键不一致）。现在 `W`/`S` 也能上下切换选项，`回车`/`E` 确认。
- 实现：`ui_up` / `ui_down` 这两个内置 action **原本只绑了方向键**（`project.godot` 里没有覆盖它们，用的是引擎默认）。现在显式声明并**追加** `W` / `S` —— 方向键仍然可用，是"追加"不是"替换"。和项目已有做法一致（`ui_accept` 之前就追加过 `E` 键）。
- 选项的键盘导航本身是引擎 `Control` 焦点系统的能力（Dialogue Manager 的 `dialogue_responses_menu.gd` 已经 `grab_focus()` 了第一个选项、并接好了上下邻居链），**不需要改插件**。真正缺的只是"W/S 没有映射到 ui_up/ui_down"。
- 实测：焦点确实落在第一个选项上，邻居链完整，按 `S S W` 的焦点轨迹为 `A→B→C→B`。

### 修复

#### 任务系统（本轮代码审查发现）
- **读档后任务进度会倒退（真 bug，两处修复）**：
  - 存档只写 `task.objectives`（含 `progress`），**不写累计账本**；而账本是运行期状态。读档后进度恢复成 3、账本却是空的 —— 下一次击杀走"把进度设为账本值"这条路，进度从 3 被改成 1，**倒退**。
    修法：新增 `TaskManager.reseed_ledger_from_tasks()`，按已恢复的进度把账本补回来（只多不少 —— 账本偏大是安全的，会被 `min` 钳在 `target`；偏小才会倒退），并在 `load_game()` 末尾调用。
  - **JSON 的数字都是 double**，`JSON.parse_string` 读回来 `progress`/`target` 变成 `3.0`/`5.0`，任务面板用 `str()` 直接显示，会变成 `3.0/5.0`。
    修法：新增 `save_manager._normalize_objectives()`，按目标要求的类型重建字典（`int()` 转换 + 保留 `name`/`target`/`source`，丢掉未知键）。
  - 实测：修复前读档再杀 1 只 → `1/5`（倒退）；修复后 → `4/5` ✓。面板显示 `3/5` 而非 `3.0/5.0` ✓。

#### 任务可能永远做不完（关键）
- 敌人**不重生**，而击杀只在"接任务之后"才累加 —— 玩家若先把某片区域清光，之后接到的击杀任务就永远无法完成（哥布林在 `world` 只有 1 只、`north` 1 只，风险尤其大）。
- 改为**累计账本**：`TaskManager._ledger` 记录 `kill:种类` / `collect:物品id` / `open:chest` 的全局累计，**与接没接任务无关**；接取任务时按账本一次性补记进度。于是"先打的也算数"：先清怪的人接任务后可能立刻达标可直接交付。
- `reset_all_tasks()` 同步清空账本 —— 否则上一局打的怪会算进这一局，接任务瞬间就完成。

#### 背包 / 任务面板打不开（关键）
- 按 B / Q 没反应。
- 根因：上个版本为了防止在主菜单按 B/Q 穿透到标题画面，给两个面板的 `_process` 加了守卫 `if not is_visible_in_tree(): return`。但 **`is_visible_in_tree()` 把节点自己的 `visible` 也算进去**，而两个面板在 `_ready()` 末尾都调了 `close()`（即 `visible = false`）—— 守卫第一行就 return，**打开面板的按键永远读不到**，形成死锁。
- 实测三种情况：面板自己隐藏 → `is_visible_in_tree()` 返回 `false`（守卫误拦）；HUD 那个 CanvasLayer 隐藏 → 也返回 `false`（这个是原意要拦的）。
- 修法：改成只查**祖先链**（`_ancestors_visible()`，不含自己），三种情况分别得到 真 / 假 / 真，语义正确。
- ⚠️ 实现上必须**同时判 `CanvasItem` 和 `CanvasLayer`**：`CanvasLayer` **不是** `CanvasItem`，只查 `is CanvasItem` 会漏掉 HUD 那一层，主菜单里按 B/Q 又会穿透回来。

#### 女性冒险者无法交互
- 症状：靠近没有提示、按 E/回车都没反应。两个冒险者（`world` 与 `east`）都坏。
- 根因：`Boxes/speak_box.tscn` 里的 `CollisionShape2D` 是**空节点**（没有 `shape`），用它的场景必须自己填。村长的 `npc.tscn` 填了，而 `female_adventurer.tscn` **漏填** —— 没有碰撞形状，`get_overlapping_bodies()` 永远为空，`player_near` 恒为 false，所以提示不显示、按键也不响应。
- 修法：给 `female_adventurer.tscn` 补 `CircleShape2D`（半径 **40**，与村长一致，也正是原始素材包里的值）。该场景两个冒险者共用，补一处即修好两处。

#### 地图生成器仍在使用已废弃的 `task_id`
- `tools/generate_maps.gd` 的 `east` spec 还是 `{"task_id": ""}`，且 `inst.set("task_id", ...)`。`task_id` 已改为 `task_ids`（`PackedStringArray`），这个 key 在场景里早已是未识别属性、被引擎静默丢弃。
- **更严重的是**：生成器**完全没有设置 `dialogue_path`**。一旦重跑生成器覆盖 `map_east.tscn`，东原冒险者会退回默认的 `Test.dialogue` —— **用村长的台词说村长的任务**，整条东原探险线丢失。
- 修法：spec 补齐 `dialogue_path` / `task_ids` / `all_done_title`；写属性的方式从逐个 `set()` 改为遍历 spec 里除 `pos` 外的所有键（属性名写错会直接报错，而不是静默失效）。

#### 其他
- **任务目标改为数据驱动**：原先 `notify_enemy_killed` 里硬编码 `match TASK_KILL_BATS`，加一种怪就要改路由代码。现改为目标自带 `source` 字段（如 `kill:goblin`），`notify_progress()` 自动推进所有匹配的进行中任务 —— **新增任务只需写 `.tres` + 登记，不动任何代码**。
- **敌人广播的语义修正**：`bat.gd` 原先广播 `TASK_KILL_BATS`（任务 id 当敌人 id 用），多任务并存后含义就串了。现统一广播 `ENEMY_*` 敌人种类常量。
- **两个 NPC 脚本的重复逻辑收敛**：`npc.gd`（`CharacterBody2D`）与 `female_adventurer.gd`（`KinematicActor`）**没有共同基类**，任务链判定若各写一遍必然走样。现统一调用 `TaskManager.chain_title()` / `next_in_chain()` / `consume_chain_requests()`。
- **文档与代码不符**：`CLAUDE.md` 里写的任务是 `task_ui.gd` / `UI/task_ui.gd`，**该文件并不存在**（实际是 `UI/task_panel.gd`）。已更正。

### 变更
- NPC 的 `task_id`（单个字符串）改为 **`task_ids`（`PackedStringArray` 串行链）**，新增 `all_done_title`。旧场景里的 `task_id = "..."` 会成为未识别属性被引擎静默丢弃 —— 三处 NPC 已全部改掉。
- 敌人新增事件钩子：`acornback` / `goblin` / `mimic` / `slime` 的 `_die()` 补上击杀广播（此前只有 `bat` 有）；`Player.collect()` 广播采集；`chest._open()` 广播开箱。
- **HUD 布局调整**：
  - **金币余额移到左下角**（原在右上角）：`right -6, top 6` → 锚点改为左下 `6, -22`。屏幕四角分工：左上血量 / 右上小地图 / 左下金币 / 右下物品计数。
  - **小地图贴到最右上角**：上边距 `26` → **`6`**。它会压住 toast 浮条的底板（底板 y 22..46、x 10..310），但**量过 toast 的实际文字宽度，最长只到 x≈218，钻不到小地图下面**（小地图左边界 238），所以提示内容不会丢。

### 清理
- 删除仓库根目录的 Godot 临时文件 `world.tscn3208196381.tmp`（56KB，此前一直被跟踪在 git 里）。

### 已知问题与死代码
- **`素材/ui素材/` 里的 20 个 UI 素材在本机加载不出来**：`FileAccess.file_exists()` 与 `DirAccess.open("res://素材")` 都失败（但 `DirAccess.open("res://").get_directories()` 却能列出 `素材`）。实测是 **Godot 4.7.2 在本机这个中文 locale 下解析不了中文路径** —— `.import` 与 `.godot/imported/*.ctex` 都在，但 `load()` 全部返回 `null`。**要用这批素材必须先移到纯 ASCII 路径**（如 `UI/components/`）。小地图因此**手绘**边框，不依赖这批素材。
- **`UI/dialogue_balloon.tscn` 改了不会生效**：NPC 调的是 `DialogueManager.show_example_dialogue_balloon()`，该方法按**视口宽度**自动选插件自带的 balloon（本项目 320 < 400 → `addons/dialogue_manager/example_balloon/small_example_balloon.tscn`），完全绕过项目和 `DMSettings`。改对话外观要改插件那份（或改成 `show_dialogue_balloon()` 走 `DMSettings.balloon_path`）。
- **以下死代码未删除，仅记录**（都是本会话之前就存在的，删不删请你定）：
  - `title_screen.tscn` / `title_screen.gd`：v1.6 之前的主菜单，已被 `UI/main_menu.tscn` 取代，无任何引用。其脚本还是"删存档 + 切 world.tscn"的旧流程，早于 `MapManager`。
  - `UI/ecg_ui.tscn` / `UI/ecg_ui.gd`：心电图健康显示实验，引用 `PlayerStates`，从未被实例化。
  - 旧存档兼容分支（按 `item_name` 回退查找、目标缺 `source` 的兜底）保留着，但没有生产环境存档。

### 说明（未改动任何游戏逻辑）
- 新地图**不需要为小地图做额外适配**，只要满足：有名为 `CliffTileMap` 的 TileMap、崖壁是闭合外环、关口位置记在 `EdgeTriggers/<名字>/CollisionShape2D` 上。三条已写进 `地图布局参考.md`。

## v1.7 - 2026/09/13

### 新增
- **多地图系统**：世界从 1 张图扩为 **3 张独立大地图**，走到地图边缘自动切换。
  - `world`（`res://world.tscn`，村口田野，原有）+ **`north`**（`maps/map_north.tscn`，北坡羊道，1536×800）+ **`east`**（`maps/map_east.tscn`，东原营地，1536×800）。
  - **边缘传送**：走到地图边缘的关口即切到相邻地图。`maps/edge_trigger.gd`（`Area2D`，`collision_mask = 2` 只探玩家层）。关口两侧同名 —— world 的 `NorthPass`/`EastPass` ↔ north/east 的 `Spawns/NorthPass`、`Spawns/EastPass`。
  - **内容量**：world 20 只怪 + 6 宝箱；north 19 只怪 + 3 宝箱；east 30 只怪 + 4 宝箱 + 1 名支线冒险者。
- **`MapManager` autoload**（`maps/map_manager.gd`）：地图注册表、切图、跨图出生点定位，以及"本局是否第一次进图"的判定。
- **`HUD` autoload**（`UI/hud.tscn`）：HUD 从地图场景中提出，三张图共享一份；地图外（主菜单）整体隐藏。
- **地图生成器**（`tools/generate_maps.gd`，开发工具，不参与游戏运行）：程序化生成两张新地图。**完全确定性**（无随机数，用黄金角螺旋散布），同样的参数必得同样的结果。
- **`地图布局参考.md`**：把 `world.tscn` 的布局逆向整理成生成新地图的规范（素材图集语义、岔路结构、怪群分布规律、崖壁图块规则、必须遵守的约束）。
- **玩家血量上限 5 → 50**（`Player/player_states.tscn`），方便跑图测试。血量 HUD 增加**数值显示** `当前/上限` —— 心形图标每个 15px 宽，50 滴血要 750px、远超 320px 视口，所以心形条最多画 10 颗并按比例缩放（上限 150px），精确数值由文字承担。
- **玩家攻击击退加强 50%**：`Player/player.tscn` 的 `SwordHitBox` 覆盖 `knockback_strength = 150.0`（原为脚本默认 100）。只改玩家这一侧；敌人 `HitBox` 仍是 100，即"敌人打玩家"的击退不变。

### 修复
- **换图会清空存档（关键）**：`Player._ready()` 原本无条件调用 `_reset_new_game()`，而 `Player` 是每张地图的场景子节点 —— 每次切图都会把血量/背包/金币/任务全部清掉。现改为只在 `MapManager.consume_fresh_start()` 为真（本局第一次进图）时重置。
- **崖壁图块到处是缺口（关键）**：生成的新地图崖壁一段段断开、不连续。
  - 根因一：`set_cells_terrain_connect()` 在这个图集上会乱选图块 —— 实测把一个**纯外环**喂给它，会选出 `(5,1)(6,1)(5,2)(6,2)(4,1)(8,1)(9,1)(5,4)` 等 22 种"碎石/立柱"图块（主世界只用 11 种）。该图集的 terrain peering bits 并不自洽。
  - 根因二：我额外加了内部崖壁，而**主世界的崖壁是纯外环**（内部格数实测 = 0）。
  - 修法：改用 `_place_cliff_tiles()` **照主世界逐格指定图块**（规则从 world 逆向并逐格验证通过），并去掉内部崖壁。结果三张地图的崖壁图块全部落在主世界认可的 11 种之内，**非法图块 0 个**。
- **world 地图有 7 个游离崖壁格**：环外散落着实验遗留的崖壁图块，其中一个（格 24,0 / 世界 px 768,0）**在可玩区内**，会变成一堵莫名其妙的墙。已清除并重建外环（207 → 200 格，环完整、两个关口连通）。
- **切图时物理回调内释放碰撞体**：`goto_map()` 是从 `Area2D.body_entered` 进来的（物理回调），而 `change_scene_to_file()` 会立刻释放旧场景里的大量 `CollisionObject2D`，引擎禁止这么做（`Removing a CollisionObject node during a physics callback is not allowed`）。现改为 `call_deferred()` 延后到物理帧外执行；`_locked` 仍是同步置位，同帧重复触发照样被挡下。
- **切图刷屏 "Signal ... is already connected"**：程序化生成场景时若把 `owner` 递归设到实例化子场景的内部节点上，`pack()` 会把子场景内部的 `[connection]` 一并烙进父场景；子场景自己已经连过一遍，加载时就报 `ERR_INVALID_PARAMETER`。现改为只给生成器自建的节点（`_new_node` 登记）设 owner。生成物从 283KB/439KB 降到 34KB/45KB。
- **生成的地图丢玩家节点**：`RemoteTransform2D` 无法随实例化节点一起被 `pack()` 序列化（`owner` 设成地图根或实例根都会被静默丢弃，已实测）。现改由 `maps/world_map.gd::_ensure_camera_follow()` 在运行时补挂（已存在则跳过，兼容手工编辑的 `world.tscn`）。
- **进入新地图后相机不跟随**：`remote_path` 是**相对 RemoteTransform2D 自己**解析的，而 RT 是 Player 的子节点，两者差一层 —— 必须用 `rt.get_path_to(cam)` 得到 `../../Camera2D`（原先用 `player.get_path_to(cam)` 得到 `../Camera2D`，解析不到）。`map_manager.gd` 里"反查相机"的回退分支犯了同样的错，一并修正。
- **相机跟不紧**：`Camera2D.position_smoothing_speed` 一直是 Godot 默认的 **5.0**，而该属性单位是**像素/秒**。角色移动 100 px/秒、相机只走 5 px/秒，走得越远偏得越多。中途试过两版动态调速都不理想（其中一版依赖 `Camera2D.get_viewport_rect()`，该 API 返回的是**当前环境**的视口，编辑器里实测 1920×1001 而非游戏的 320×180，取错后钳位区间 min>max、`clampf` 返回垃圾值）。**最终方案：直接关闭位置平滑，硬跟随**，行为完全可预测。
- **相机边界比可走区小一圈（"走一段就不跟随"）**：`Camera2D` limits 原本设在崖壁带正中，而崖壁环有 2 格厚 —— 相机中心只能移动到 `x -32..672 y 58..550`，但玩家能走到 `x -161..799 y 0..608`。现把 limits 放宽到**可走区 ∓ 半视口**，两者完全一致（world：`-321, -90, 959, 698`）。
- **地图锚点未归零**：`world` 根节点曾被设成 `position = (223, 63)`，导致所有子内容整体偏移，与相机边界/背景错位。现归零。
- **背景盖不住地图（只画出第四象限）**：`Sprite2D.region_rect` 的 `position` 是**纹理偏移**，**不是世界坐标定位** —— 绘制起点永远是节点自身的 `position`。之前把世界坐标写进了 `region_rect.position`，实际只画出了 `x 0..1408 y 0..832` 这一块。现改为 `region_rect = Rect2(0, 0, w, h)` + 节点 `position` 定位。同时去掉了背景上的**非整数缩放**（会让像素画发虚）。
- **读档位置还原一直是空操作**：`load_game()` 原先从主菜单调用，此时 `current_scene` 是菜单，`get_node_or_null("Player")` 返回 null。现改由 `MapManager.install()` 在地图就位后调用。
- **主菜单误响应 B / Q**：HUD 常驻后，`inv_ui.gd` 与 `task_panel.gd` 的 `_process` 在主菜单也会轮询 `openInv`/`openTask`。现用 `is_visible_in_tree()` 加守卫。
- **崖壁环四面封闭**：原 `world.tscn` 的崖壁是完整闭合的矩形环（四面缺口数均为 0），根本无法走出去。现开出 `NorthPass`（顶部）与 `EastPass`（右侧）两个口。
- **`map_manager.gd` 类型推断报错**：`var saved := SaveManager.peek_map_id(...)` 跨 autoload 无法推断类型，改为显式 `var saved: String`。
- **`inventory/inv_UI.gd.uid` 丢失**（场景里按 `uid://dtplopkhgondo` 引用该脚本），已从 git 恢复。

### 优化
- **地图生成器按主世界的布局规律重写**（原先是均匀网格撒点，呆板且不像世界地图）：
  - **道路**：一条贯通的主干道 + 若干条长度/宽度各异的垂直岔路，部分岔路末端展宽成广场/院子（对齐 world 的岔路结构）。
  - **敌人**：从均匀撒点改为「几处混搭兵种的怪群 + 若干散兵」，对齐 world 的分布形态。
  - 道具改用「黄金角螺旋 + 避开泥路」散布。
- 两个 TileSet 从 `world.tscn` 的内嵌 `[sub_resource]` 抽成独立资源 `World/DirtTileset.tres` / `World/CliffTileset.tres`，三张地图共用。

### 已知缺口
- 两张新地图挂在 world 的边缘上，但**游戏内没有任何提示告诉玩家可以走出去**（项目里没有路牌道具，`inventory/item/signpost.tres` 是物品不是场景）。想加引导得靠村长对话加一句或新增美术。

## v1.6 - 2026/09/09

### 新增
- **UI 系统重制**：使用 Kenney UI Pack 素材重新设计整个 UI 系统。
  - **主界面** (`UI/main_menu.tscn`)：1280×720 背景图铺满、按钮缩小并底移、移除标题文字。
  - **对话框** (`UI/dialogue_balloon.tscn`)：使用 panel_border_brown 边框 + 打字机效果 + 响应选项。
  - **任务面板** (`UI/task_panel.tscn`)：使用 panel_border_brown 边框 + 滚动列表。
- **新场景素材**：
  - **篝火** (`World/campfire/`)：1280×720 背景贴图，攻击切换燃烧/熄灭状态（结构镜像敌人 bat）。
  - **游戏封面** (`UI/title/background.png`)：1280×720 主界面背景。

### 优化
- **警告修复**：
  - 未使用参数加下划线前缀（`delta` → `_delta` 等）。
  - UID 不匹配修正（bush/grass/tree/health_ui 等场景）。
  - 变量遮蔽修正（bat.gd 中 `player` 变量）。
- **素材整理**：`素材/` 文件夹内容全部迁移到对应工程目录并删除原文件夹。
- **输入系统**：新增 `interact` 输入（E 键）用于场景交互。

## v1.5 - 2026/09/08

### 新增
- **女性冒险者 NPC**：实装新 NPC——女性冒险者（FemaleAdventurer），使用 "The Female Adventurer - Free" 素材包。
  - **6 方向动画系统**：Down / Up / Left_Down / Left_Up / Right_Down / Right_Up 六个朝向，每个方向独立的 Idle/Walk 动画（共 12 个动画）。
  - **方向切换**：根据移动速度向量角度自动计算朝向，播放对应方向动画；静止时保持最后朝向。
  - **随机游走**：内置 Timer 驱动随机游走（IDLE/WANDER 状态机），在出生点附近徘徊。
  - **对话交互**：玩家靠近 SpeakBox 范围时显示"按Enter对话"提示，按 Enter 触发对话（DialogueManager）。
  - 素材入库 `NPC/FemaleAdventurer/`（12 个导入纹理 + source_pack/ 原始资源包）。
- **地图左扩**：地图向左扩展 5 格（160px）。
  - 峭壁边界从 x=-2/-1 移至 x=-7/-6。
  - 相机 limit_left 从 -32 调整为 -192。
  - 背景 region_rect 从 (0,0,800,600) 调整为 (-160,0,960,600)。
  - 新增 15 草丛 + 6 灌木 + 6 树装饰布置于左侧新区域。

### 优化
- **素材目录整理**：`素材/` 文件夹全部迁移至对应工程目录并删除原文件夹。
  - `素材/The Female Adventurer - Free/` → `NPC/FemaleAdventurer/source_pack/`
  - `素材/mobs/{goblin,mimic,slime}/` + `素材/mobs/gif/` → `Enemies/mobs/source/`
  - `素材/ui/` → `UI/`

## v1.4 - 2026/09/07

### 新增
- **三种新敌人·通用 Mob 基类**：实装哥布林（Goblin）、宝箱怪（Mimic）、史莱姆（Slime），共用 `Enemies/mob.gd` 通用地面敌人基类（`IDLE/WANDER/CHASE` 状态机，运行时按 64px 横条 sheet 自动切帧建 SpriteFrames）。
  - **哥布林**：发现玩家即挥击追击的标准近战敌人。
  - **宝箱怪**：平时伪装成物件静止（`Disguise` 动画），玩家靠近才苏醒追击。
  - **史莱姆**：缓慢弹跳接近玩家后攻击。
  - 死亡统一流程：播完 `Death` 动画 → 爆炸特效 + 掉落（金币 + 可选物品）→ 销毁。
  - 素材入库 `Enemies/mobs/{goblin,mimic,slime}/`（各动作横条 sheet）；场景 `Enemies/goblin.tscn` / `mimic.tscn` / `slime.tscn` 由 `acornback.tscn` 模板克隆改写。
- **世界布置**：地图新增 3 哥布林 + 2 宝箱怪 + 3 史莱姆（与原有 14 蝙蝠、3 橡子甲虫、6 宝箱共存）。

## v1.3 - 2026/09/07

### 修复
- **素材目录清理迁移**：`素材/` 下 500+ 原始素材仅 9 个被代码实际引用；将这 9 个活跃文件迁移到稳定工程目录（`Effects/`、`UI/`、`inventory/`、`World/`），删除 `素材/` 文件夹，修正 CLAUDE.md 过时说明。

## v1.2 - 2026/09/07

### 新增
- **世界宝箱·开箱掉落 16 件 RPG 道具**：地图布置 6 个银箱（村口/田间/森林/角落/近东）；挥剑开启 → 开箱动画 → 扇形掉出 1~2 件该档掉落池的 RPG 道具（世界拾取物入背包）。
  - 宝箱素材来自 `素材/新增/Treasure Boxes Pixel art/Silver/`（8 帧开箱动画），已按帧拼成 `Enemies/chest/chest_silver.png` 竖条图入库，运行时分格播放。

### 修复
- **挥剑声音不同步/两段声**：`swing_effect` 里多余的 `Swipe.wav` autoplay 去掉，挥剑声只由玩家攻击动画音轨（0.1s）播一次。
- **金币拾取物过大**：金币动画缩小为原来的 75%（落地弹跳同步等比）。
- **宝箱开箱到一半贴图消失**：素材拼图曾因解码器步长 bug 只剩前两帧有画——已重拼，8 帧完整开箱动画。
- **宝箱收尾打磨**：外观统一为银箱（`variant` 只决定掉落池，不影响画风）；`@tool` + 运行时切帧使编辑器里也能显示贴图；开箱动画放慢（帧时长/速度常量可调）；开完即销毁、掉落拾取物缩小到 75%。
- **橡子甲虫滚球朝向**：`Roll_Attack` 帧相对行走是镜像的，旧翻转逻辑导致滚向玩家时永远背朝玩家——翻转极性按状态取反，改为正面朝向玩家。
- **橡子甲虫死亡动画**：死亡不再秒爆——先播完 `Death` 动画（关闭受击/碰撞/移动），播完后才生成爆炸特效、掉落并销毁。

## v1.1 - 2026/09/06

### 新增
- 实装新敌人 **橡子甲虫（Acornback_Rollbeast）**：IDLE/WANDER/ROLL 状态机，发现玩家卷成球高速冲撞，已布入世界地图。
- **武器挥砍/受击特效**：挥砍刀光 `swing_effect` + 受击反馈 `hit_effect`（60% 大小），随攻击方向定位/旋转。
- **16 件 RPG 道具**：背包、睡垫、帐篷、篝火、铁锅、铁钥匙、宝箱、钱袋、宝石、地图、罗盘、火把、水桶、路标、钩爪、望远镜（`inventory/item/` + 图标 `inventory/icons/`，已登记进物品注册表）。
- **金币系统**：`Wallet` 全局钱包（Autoload）+ 世界金币拾取物（旋转动画/拾取/超时淡出）+ HUD 右上角金币余额；**杀敌掉落**——蝙蝠 1~3、橡子甲虫 2~5；金币随存档读写、新开局重置。

### 优化
- **背包 12 格 → 30 格**（6×5 界面）。
- 素材目录重组：原版素材统一移入 `素材/原版/`，第三方原始素材 `素材/新增/` 不入库（.gitignore）。
- 项目纹理过滤改 **Nearest**，像素画更锐利。

### 修复
- **蝙蝠靠近玩家自伤**：剑判定盒由动画轨误启用改为代码开关（仅挥刀瞬间开启）。
- **刀光方向**：朝左挥砍时由"从下往上"修正为"从上往下"（纯左向改用水平镜像）。
- **阴影分层**：角色/小树/大树/新敌人阴影不再浮在身体上层（身体 z_index=1，阴影垫底）。
